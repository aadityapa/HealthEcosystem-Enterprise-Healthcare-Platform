# Template — replace placeholders before applying. Do not commit real secrets.
# Recommended: use External Secrets Operator or Sealed Secrets in production.
#
# kubectl create secret generic health-platform-secrets \
#   --namespace health-platform \
#   --from-literal=DATABASE_URL='postgresql://health:REPLACE@postgres:5432/healthecosystem?schema=public' \
#   --from-literal=JWT_SECRET='REPLACE_WITH_256_BIT_SECRET' \
#   --from-literal=INTERNAL_SERVICE_KEY='REPLACE_WITH_SERVICE_KEY' \
#   --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: health-platform-secrets
  namespace: health-platform
  labels:
    app.kubernetes.io/part-of: healthecosystem
type: Opaque
stringData:
  DATABASE_URL: postgresql://health:CHANGE_ME@postgres.health-platform.svc.cluster.local:5432/healthecosystem?schema=public
  JWT_SECRET: CHANGE_ME_USE_256_BIT_SECRET
  INTERNAL_SERVICE_KEY: CHANGE_ME_IN_PRODUCTION
